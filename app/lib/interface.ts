export interface blogCard {
    title: 'string',
    metaDescription: 'string',
    currentSlug: 'string',
    featureImage: any,
    content: any,
    tag: 'string',
}

export interface tag {
    tag: string,
    currentSlug: string,
    _id: string,
}